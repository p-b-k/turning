////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Try a generic loader that reads the tape from the input
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct AppConfig {
    delay: u32,
}

impl AppConfig {
    pub fn new() -> AppConfig {
        AppConfig { delay: 100 }
    }
}

fn main() {
    let cfg = AppConfig::new();
}
